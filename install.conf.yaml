- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.gitconfig:
    ~/.zshrc: 

- shell:
  - [git submodule update --init --recursive, Installing submodules]
